name: Build EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install flask bleak pyinstaller

    - name: Build EXE
      run: |
        pyinstaller --onefile --noconsole --add-data "templates;templates" --name "XOSSMonitor" app.py

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: XOSSMonitor
        path: dist/XOSSMonitor.exe
